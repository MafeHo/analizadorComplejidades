Hanoi(n)
begin
    If (n > 0) then
    begin
        // Mover n-1 discos al auxiliar
        CALL Hanoi(n-1);
        
        // Mover disco base (Costo constante)
        movimiento <- 1;
        
        // Mover n-1 discos al destino
        CALL Hanoi(n-1);
    end
end
