MatrizRecursiva(n)
begin
    If (n > 1) then
    begin
        mitad <- n / 2;
        
        // 8 llamadas recursivas
        CALL MatrizRecursiva(mitad);
        CALL MatrizRecursiva(mitad);
        CALL MatrizRecursiva(mitad);
        CALL MatrizRecursiva(mitad);
        CALL MatrizRecursiva(mitad);
        CALL MatrizRecursiva(mitad);
        CALL MatrizRecursiva(mitad);
        CALL MatrizRecursiva(mitad);
        
        // Sumar matrices (n*n)
        for i <- 1 to n do begin
            for j <- 1 to n do begin
                suma <- 1;
            end;
        end;
    end
end
